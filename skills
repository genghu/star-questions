增加一个功能：每次学生回答完一套题时（页面上的所有题目），我们使用正确错误的图标来表示学生的答题结果。如果该套题目中，学生每道题目的答题结果均为正确，我们用正确图标来记录答题结果，如果没有全部正确，我们用错误图标记录答题结果。图标圆形、实心，上面有正确错误符号，并用绿色表示正确、红色表示错误。图标出现的顺序是学生做题的顺序，正确错误是结果


1. 将题目的参数增加到20组，每组参数使用不同的问题背景，使问题背景多样化。
2. 将刷新参数改成“下一题”，由“下一题”按钮控制题目刷新，所以下一题按钮控制文档内所有问题参数的刷新（如果文档有多个问题的话），将“下一题”按钮放在“重新作答”按钮的位置，删除“重新作答”按钮
3. 提交答案之后不可重复提交
4. 提交答案之后出现“查看/隐藏答案解析”按钮，控制显示和隐藏答案解析
5. 将答题结果中的内容改为正确错误的图标来表示学生做了几套题（一套题目是指该文档中的所有题目），每套题目的结果只有正确或错误。页面使用正确错误图标来记录学生做的每一套题的结果。图标为圆形、实心，上面有正确错误符号，并用绿色表示正确、红色表示错误。图标出现的顺序是学生做题的顺序，正确错误是结果（每一套题，只有学生答对所有的问题，该套题的答题结果才算正确）

1. 将当前文档的问题参数扩展20组，每组参数设置不同的问题背景。
2. 根据“总体均值差异的置信区间标准正态分布应用.html”的样式改造当前页面。
3. 将"t分布.html"中的t分布表移植到当前页面（让t分布表按钮置于容器最左侧，另外两个按钮居中）。
4. 将上面的所有步骤完成后，检查所有的参数组生成的问题的答案和答案解析是否正确


1. 将当前文档的问题参数扩展20组，每组参数设置不同的问题背景。
2. 根据“总体均值假设检验-Z检验.html”的样式改造当前页面。
3. 将"t分布.html"中的t分布表移植到当前页面（让t分布表按钮置于容器最左侧，另外两个按钮居中）。
4. 将上面的所有步骤完成后，检查所有的参数组生成的问题的答案和答案解析是否正确

作为一个语言学家和统计教育学家，请仔细阅读每组参数的题目背景、配对设计说明和要求。找出语言上不合理的地方并修改。